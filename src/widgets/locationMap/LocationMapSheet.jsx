/* ─── Location Map — Sheet (placeholder) ─────────────────────────
   Region 5 fills this in: shared shell (scrim, FLIP lift, header bar,
   close button, footer slot) wrapping a variant-specific body.
   Region 1 ships only the React.lazy import target so LocationMap.jsx
   can wire the sheet open/close infrastructure ahead of time.
   ─────────────────────────────────────────────────────────────── */

export function LocationMapSheet() {
  return null
}

export default LocationMapSheet
